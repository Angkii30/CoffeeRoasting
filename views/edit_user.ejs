<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Coffee Roasting</title>

    <link rel="icon" href="/Picture/logo.png">
    <link rel="stylesheet" href="/CSS/edit.css">
</head>

<body>
    <main class="main">
        <header class="topbar"></header>



        <div class="container">
            <div class="card_user">
                <div class="card-header-user">
                    เพิ่มสิทธ์การใช้งานระบบ
                </div>

                <div class="card-body">

                    <!-- ✅ FORM สำคัญมาก -->
                    <form action="/update" method="post">

                        <!-- ส่ง user_id -->
                        <input type="hidden" name="user_id" value="<%= user.user_id %>">

                        <div class="form-row-user">
                            <label>ชื่อผู้ใช้ :</label>
                            <input type="text" name="username" value="<%= user.username %>" readonly>
                        </div>

                        <div class="form-row-user">
                            <label>รหัสผ่าน :</label>
                            <input type="password" name="password" placeholder="ไม่กรอก = ไม่เปลี่ยน">
                        </div>

                        <div class="form-row-user">
                            <label>สิทธิ์การใช้งาน :</label>
                            <select name="role">
                                <option value="admin" <%=user.role==='admin' ? 'selected' : '' %>>แอดมิน</option>
                                <option value="owner" <%=user.role==='owner' ? 'selected' : '' %>>เจ้าของ</option>
                                <option value="staff" <%=user.role==='staff' ? 'selected' : '' %>>ผู้ช่วย</option>
                                <option value="sales" <%=user.role==='sales' ? 'selected' : '' %>>พนักงานขาย</option>
                            </select>
                        </div>

                        <div class="actions">
                            <button type="submit" class="btn save">บันทึก</button>
                            <button type="button" class="btn cancel" onclick="history.back()">กลับ</button>
                        </div>

                    </form>
                    <!-- END FORM -->

                </div>
            </div>
        </div>
    </main>
    <script src="/JS/edit_user.js"></script>

</body>

</html>